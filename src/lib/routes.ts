/** Client routes — keep in sync with `App.tsx` and GitHub Pages SPA `404.html` copy. */
export const ROUTES = {
  home: "/",
  terms: "/terms",
  privacy: "/privacy",
  contact: "/contact",
} as const;
